import CrmModulePage from "@/components/CrmModulePage";

export default function SettingsPage() {
  return (
    <CrmModulePage
      title="Settings"
      description="Configure workspace preferences, billing-ready controls, user defaults, security, and CRM behavior."
    />
  );
}
